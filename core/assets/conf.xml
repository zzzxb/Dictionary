<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties resource="db.properties"></properties>
    <typeAliases>
        <package name="src.com.mini.dictionary.entity"/>
    </typeAliases>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <!--这里设置jdbc驱动类的名称-->
                <property name="driver" value="${driver}"/>
                <!--数据库连接字符串，本地连接，端口号，数据库名-->
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/><!--用户名-->
                <property name="password" value="${password}"/><!--密码-->
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <!--注册映射文件的路径-->
        <mapper resource="src\com\mini\dictionary\mapper\EnglishChineseDictionaryMapper.xml"/>
    </mappers>
</configuration>
